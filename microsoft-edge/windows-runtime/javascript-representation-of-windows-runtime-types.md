---
title: Representação JavaScript de tipos do Windows Runtime
ms.custom: ''
ms.date: 04/01/2017
ms.prod: microsoft-edge
ms.reviewer: ''
ms.suite: ''
ms.technology: windows-integration
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
manager: ''
ms.openlocfilehash: b76c28d3448b8be6fbef1fd7e23b07b2eff8d087
ms.sourcegitcommit: 6860234c25a8be863b7f29a54838e78e120dbb62
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 04/09/2020
ms.locfileid: "10562521"
---
# <span data-ttu-id="a973b-102">Representação JavaScript de tipos do Windows Runtime</span><span class="sxs-lookup"><span data-stu-id="a973b-102">JavaScript Representation of Windows Runtime Types</span></span>  

<span data-ttu-id="a973b-103">A tabela a seguir descreve a maneira como o JavaScript representa tipos do Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="a973b-103">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="a973b-104">Os recursos do tempo de execução do Windows não estão disponíveis para aplicativos que são executados no Internet Explorer.</span><span class="sxs-lookup"><span data-stu-id="a973b-104">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <span data-ttu-id="a973b-105">Tipos de tempo de execução do Windows em JavaScript</span><span class="sxs-lookup"><span data-stu-id="a973b-105">Windows Runtime Types in JavaScript</span></span>  

| <span data-ttu-id="a973b-106">Tipo de tempo de execução do Windows</span><span class="sxs-lookup"><span data-stu-id="a973b-106">Windows Runtime type</span></span> | <span data-ttu-id="a973b-107">Representação JavaScript</span><span class="sxs-lookup"><span data-stu-id="a973b-107">JavaScript representation</span></span> | <span data-ttu-id="a973b-108">Descrição</span><span class="sxs-lookup"><span data-stu-id="a973b-108">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="a973b-109">Um tempo de execução do Windows `Uint8` é um inteiro de 8 bits não assinado, representado como um número JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-109">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="a973b-110">Um valor de JavaScript é primeiramente convertido em um número de JavaScript e, em seguida, o `ToUint32` processo é seguido.</span><span class="sxs-lookup"><span data-stu-id="a973b-110">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="a973b-111">Se o valor de número JavaScript estiver fora do intervalo de um Uint8, o resultado terá o módulo 2 ^ 8 aplicado.</span><span class="sxs-lookup"><span data-stu-id="a973b-111">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span> |  
| `Int32` | `Number` | <span data-ttu-id="a973b-112">Um tempo de execução do Windows `Int32` é um inteiro de 32 assinado assinado, representado como um número JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-112">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="a973b-113">Um valor de JavaScript é primeiramente convertido em um número de JavaScript e, em seguida, o `ToInt32` processo é seguido.</span><span class="sxs-lookup"><span data-stu-id="a973b-113">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="a973b-114">Se o valor de número de JavaScript estiver fora do intervalo de um `Int32` , o resultado terá o módulo 2 ^ 16 aplicado.</span><span class="sxs-lookup"><span data-stu-id="a973b-114">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span> |  
| `Int64` | `Number` | <span data-ttu-id="a973b-115">Um tempo de execução `Int64` do Windows é um inteiro de 64 assinado, representado como um número padrão quando ele está dentro do intervalo \ [-2 ^ 53, 2 ^ 53 \].</span><span class="sxs-lookup"><span data-stu-id="a973b-115">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="a973b-116">Se ficar fora desse intervalo, ele será representado como um valor numérico que tem um repositório de backup personalizado que mantém os bits de 64 completos de dados inteiros.</span><span class="sxs-lookup"><span data-stu-id="a973b-116">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="a973b-117">Todas as operações matemáticas nesses valores numéricos de Int64 personalizados fazem com que o valor seja convertido em um número padrão no intervalo \ [-2 ^ 53, 2 ^ 53 \].</span><span class="sxs-lookup"><span data-stu-id="a973b-117">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="a973b-118">Se o valor estiver fora desse intervalo, um erro de tipo será gerado.</span><span class="sxs-lookup"><span data-stu-id="a973b-118">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="a973b-119">As exceções a esse processo de conversão são `==` as `===` `SameValue` operações, e e `RelationalComparison` que comparam os argumentos com base nos bits 64 completos quando passaram dois valores de 64 bits representados.</span><span class="sxs-lookup"><span data-stu-id="a973b-119">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="a973b-120">Se o argumento for um número padrão do JavaScript, essas operações também converterão o argumento em um número JavaScript antes da comparação.</span><span class="sxs-lookup"><span data-stu-id="a973b-120">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="a973b-121">Um valor JavaScript será diretamente atribuído se for um valor Int64 em si; caso contrário, o resultado da aplicação da `ToInteger` conversão no valor será passado para o tempo de execução do Windows.</span><span class="sxs-lookup"><span data-stu-id="a973b-121">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="a973b-122">Se a coerção de tipo falhar, uma exceção será retornada.</span><span class="sxs-lookup"><span data-stu-id="a973b-122">If the type coercion fails, an exception is returned.</span></span> |  
| `Uint64` | `Number` | <span data-ttu-id="a973b-123">Um tempo de execução `Uint64` do Windows é um inteiro de 64 bits sem sinal, representado como um número padrão quando ele está dentro do intervalo \ [0, 2 ^ 53 \].</span><span class="sxs-lookup"><span data-stu-id="a973b-123">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="a973b-124">Se ficar fora desse intervalo, ele será representado como um número que tem um repositório de backup personalizado que mantém os bits 64 completos de dados inteiros sem sinal.</span><span class="sxs-lookup"><span data-stu-id="a973b-124">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="a973b-125">Todas as operações matemáticas nesses valores de UInt64 personalizados fazem com que o valor seja convertido em um número padrão no intervalo \ [0, 2 ^ 53 \].</span><span class="sxs-lookup"><span data-stu-id="a973b-125">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="a973b-126">Se o valor estiver fora desse intervalo, um erro de tipo será gerado.</span><span class="sxs-lookup"><span data-stu-id="a973b-126">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="a973b-127">As exceções a esse processo de conversão são `==` as `===` `SameValue` operações, e e `RelationalComparison` que comparam os argumentos com base nos bits 64 completos quando passados valores de 2 64 bits.</span><span class="sxs-lookup"><span data-stu-id="a973b-127">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="a973b-128">Se o argumento for um número padrão do JavaScript, essas operações também converterão o argumento em um número JavaScript antes da comparação.</span><span class="sxs-lookup"><span data-stu-id="a973b-128">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="a973b-129">Um valor de JavaScript será diretamente atribuído se for um valor UInt64 em si; caso contrário, o resultado da aplicação da `ToInteger` conversão no valor, seguido da criação do módulo 2 ^ 52, será passado para Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="a973b-129">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="a973b-130">Se a conversão do tipo falhar, uma exceção será retornada.</span><span class="sxs-lookup"><span data-stu-id="a973b-130">If the type conversion fails, an exception is returned.</span></span> |  
| `Single` | `Number` | <span data-ttu-id="a973b-131">Um tempo de execução do Windows `Single` é um número de ponto flutuante de 32 bits, representado como um número de JavaScript, selecionando o valor de precisão dupla mais próximo para o valor de precisão única.</span><span class="sxs-lookup"><span data-stu-id="a973b-131">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="a973b-132">Um valor JavaScript é convertido em um número JavaScript e, em seguida, o intervalo é verificado para garantir que o valor possa ser representado em um número de ponto flutuante de ponto flutuante de 32 754 bits de precisão simples.</span><span class="sxs-lookup"><span data-stu-id="a973b-132">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="a973b-133">Se a conversão ou verificação de intervalo falhar, um erro de marshaling será retornado.</span><span class="sxs-lookup"><span data-stu-id="a973b-133">If the conversion or range check fails, a marshaling error is returned.</span></span> |  
| `Double` | `Number` | <span data-ttu-id="a973b-134">Um tempo de execução do Windows `Double` é um número de ponto flutuante de 64 bits.</span><span class="sxs-lookup"><span data-stu-id="a973b-134">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="a973b-135">é usado para converter um tempo de execução do Windows `Double` em um número JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-135">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="a973b-136">Se a conversão falhar, um erro de marshaling será retornado.</span><span class="sxs-lookup"><span data-stu-id="a973b-136">If the conversion fails, a marshaling error is returned.</span></span> |  
| `Boolean` | `Boolean` | <span data-ttu-id="a973b-137">Um tempo de execução do Windows `Boolean` é um valor de 8 bits em que zero é `false` e qualquer valor diferente de zero é `true` representado como um JavaScript `Boolean` .</span><span class="sxs-lookup"><span data-stu-id="a973b-137">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="a973b-138">Um valor de JavaScript é primeiramente convertido em JavaScript `Boolean` por `ToBoolean` .</span><span class="sxs-lookup"><span data-stu-id="a973b-138">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="a973b-139">Isso significa que uma função do tempo de execução do Windows declarada como `void func(BOOL);` pode ser gravada em JavaScript como `obj.func("test");` .</span><span class="sxs-lookup"><span data-stu-id="a973b-139">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="a973b-140">A função do tempo de execução do Windows é chamada com o `BOOL` parâmetro passado como `TRUE` .</span><span class="sxs-lookup"><span data-stu-id="a973b-140">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="a973b-141">Se a conversão falhar, um erro de marshaling será retornado.</span><span class="sxs-lookup"><span data-stu-id="a973b-141">If the conversion fails, a marshaling error is returned.</span></span> |  
| `Char16` | `String` | <span data-ttu-id="a973b-142">Um tempo de execução do Windows `Char16` é um caractere Unicode de 16 bits, representado como uma cadeia de caracteres JavaScript que contém um único caractere.</span><span class="sxs-lookup"><span data-stu-id="a973b-142">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="a973b-143">Um valor JavaScript é convertido em uma cadeia de caracteres JavaScript por `ToString` e, em seguida, verificado para garantir que a cadeia de caracteres seja apenas um único caractere de longa duração.</span><span class="sxs-lookup"><span data-stu-id="a973b-143">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="a973b-144">O caractere único é passado como o `Char16` valor.</span><span class="sxs-lookup"><span data-stu-id="a973b-144">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="a973b-145">Se a verificação de conversão ou de comprimento falhar, um erro de marshaling será retornado.</span><span class="sxs-lookup"><span data-stu-id="a973b-145">If the conversion or length check fails, a marshaling error is returned.</span></span> |  
| `String` | `String` | <span data-ttu-id="a973b-146">Um tempo de execução `String` do Windows é uma sequência imutável de `Char16` objetos.</span><span class="sxs-lookup"><span data-stu-id="a973b-146">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="a973b-147">As cadeias de caracteres são representadas como `String` objetos JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-147">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="a973b-148">As cadeias de caracteres do Windows Runtime não têm valores diferentes para `null` e uma cadeia de caracteres vazia \ ("" \).</span><span class="sxs-lookup"><span data-stu-id="a973b-148">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="a973b-149">e valores de cadeia de caracteres vazias são convertidos na cadeia de caracteres vazia de JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-149">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="a973b-150">Observação: quando JavaScript `null` é passado para um parâmetro do tempo de execução do Windows que espera uma cadeia de caracteres, ele produz o valor de cadeia de caracteres "NULL", não `null` ou a cadeia de caracteres vazia \ ("" \).</span><span class="sxs-lookup"><span data-stu-id="a973b-150">Note:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="a973b-151">Cadeias de caracteres passadas para o tempo de execução do Windows sempre devem ser instanciadas para a cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="a973b-151">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span> |  
| `Enumeration` | `Object` | <span data-ttu-id="a973b-152">Um tempo de execução do Windows `Enumeration` é um inteiro de 32 bits \ (assinado ou não assinado \) que tem um conjunto associado de constantes nomeadas.</span><span class="sxs-lookup"><span data-stu-id="a973b-152">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="a973b-153">Em JavaScript, uma enumeração é representada como um objeto que contém um campo somente leitura para cada valor nomeado.</span><span class="sxs-lookup"><span data-stu-id="a973b-153">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="a973b-154">Os valores de enumeração são convertidos em números JavaScript, conforme definido anteriormente para `Int32` and `UInt32` .</span><span class="sxs-lookup"><span data-stu-id="a973b-154">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="a973b-155">Quando um valor JavaScript é convertido em uma enumeração do tempo de execução do Windows, ele é convertido, truncado e o intervalo é verificado conforme descrito anteriormente.</span><span class="sxs-lookup"><span data-stu-id="a973b-155">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="a973b-156">No entanto, o valor resultante não é verificado em relação aos valores nomeados definidos que são especificados na enumeração.</span><span class="sxs-lookup"><span data-stu-id="a973b-156">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span> |  
| `Structure` | `Object` | <span data-ttu-id="a973b-157">Um tempo de execução `Structure` do Windows é uma coleção heterogênea de campos de dados nomeados.</span><span class="sxs-lookup"><span data-stu-id="a973b-157">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="a973b-158">Em JavaScript, uma estrutura é representada como um objeto JavaScript que contém uma propriedade nomeada para cada campo da estrutura.</span><span class="sxs-lookup"><span data-stu-id="a973b-158">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="a973b-159">Se a conversão de qualquer valor de estrutura falhar, um erro de marshaling será retornado.</span><span class="sxs-lookup"><span data-stu-id="a973b-159">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="a973b-160">Os campos no objeto JavaScript que não têm equivalentes na estrutura do tempo de execução do Windows são ignorados.</span><span class="sxs-lookup"><span data-stu-id="a973b-160">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="a973b-161">Observação: uma estrutura do tempo de execução do Windows não pode ser instanciada em JavaScript com a `new` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="a973b-161">Note:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span> |  
| `Array` | `Array` | <span data-ttu-id="a973b-162">Um tempo de execução do Windows `Array` é convertido em uma matriz de JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-162">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="a973b-163">No entanto, as matrizes do Windows Runtime não são redimensionáveis, portanto, algumas operações de matriz de JavaScript não são possíveis.</span><span class="sxs-lookup"><span data-stu-id="a973b-163">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="a973b-164">A `[[Class]]` Propriedade Internal de uma matriz convertida não está definida como "array" porque isso não é permitido pela especificação ECMAScript 5.</span><span class="sxs-lookup"><span data-stu-id="a973b-164">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="a973b-165">Isso significa que `Array.isArray(v)` retorna `false` .</span><span class="sxs-lookup"><span data-stu-id="a973b-165">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="a973b-166">Um valor JavaScript é convertido em uma matriz do Windows Runtime da seguinte maneira: se o valor for `null` ou `undefined` , ele será convertido em Native `null` .</span><span class="sxs-lookup"><span data-stu-id="a973b-166">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="a973b-167">Se a `[[Class]]` propriedade interna for "array", ela será copiada para uma matriz nativa e uma referência a essa matriz será passada.</span><span class="sxs-lookup"><span data-stu-id="a973b-167">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="a973b-168">Se for uma matriz convertida, conforme descrito anteriormente, a matriz nativa subjacente será transmitida.</span><span class="sxs-lookup"><span data-stu-id="a973b-168">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="a973b-169">Observação: passar um valor de matriz JavaScript para um método do tempo de execução do Windows causa uma cópia completa da matriz.</span><span class="sxs-lookup"><span data-stu-id="a973b-169">Note:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span> |  
| <span data-ttu-id="a973b-170">Delegate \ (retorno de chamada de função \)</span><span class="sxs-lookup"><span data-stu-id="a973b-170">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="a973b-171">Um representante do tempo de execução do Windows é uma referência a um único método.</span><span class="sxs-lookup"><span data-stu-id="a973b-171">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="a973b-172">Um representante do tempo de execução do Windows é representado em JavaScript como JavaScript `Function` , que tem a garantia de ser chamado no thread adequado.</span><span class="sxs-lookup"><span data-stu-id="a973b-172">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="a973b-173">Quando `Function` invocado, os argumentos são convertidos nos tipos de parâmetro equivalentes do tempo de execução do Windows.</span><span class="sxs-lookup"><span data-stu-id="a973b-173">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="a973b-174">Se houver menos argumentos JavaScript do que `in` os parâmetros representante, a chamada de representante falhará.</span><span class="sxs-lookup"><span data-stu-id="a973b-174">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="a973b-175">Se houver mais argumentos JavaScript do que os `in` parâmetros no representante, os argumentos adicionais JavaScript serão ignorados.</span><span class="sxs-lookup"><span data-stu-id="a973b-175">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="a973b-176">Após o representante ser invocado, os `out` parâmetros são convertidos em tipos JavaScript e retornados.</span><span class="sxs-lookup"><span data-stu-id="a973b-176">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="a973b-177">Se um objeto de função JavaScript nativo for convertido em um representante do Windows Runtime, ele será encapsulado em um representante do tempo de execução do Windows do tipo correspondente.</span><span class="sxs-lookup"><span data-stu-id="a973b-177">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="a973b-178">Quando o representante é invocado, os `in` parâmetros são convertidos em tipos JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-178">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="a973b-179">Após o representante ser invocado, o valor de retorno é convertido nos `out` parâmetros do representante.</span><span class="sxs-lookup"><span data-stu-id="a973b-179">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span> |  
| <span data-ttu-id="a973b-180">Interface</span><span class="sxs-lookup"><span data-stu-id="a973b-180">Interface</span></span> | `Object` | <span data-ttu-id="a973b-181">Interfaces e grupos de interface não são representados diretamente em JavaScript como objetos.</span><span class="sxs-lookup"><span data-stu-id="a973b-181">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="a973b-182">No entanto, as interfaces são representadas como parâmetro e tipos de retorno dos métodos do tempo de execução do Windows.</span><span class="sxs-lookup"><span data-stu-id="a973b-182">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="a973b-183">Uma instância de interface do tempo de execução do Windows é convertida da seguinte maneira:</span><span class="sxs-lookup"><span data-stu-id="a973b-183">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="a973b-184">1. se houver uma classe de tempo de execução válida, represente o objeto como uma instância dessa classe.</span><span class="sxs-lookup"><span data-stu-id="a973b-184">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="a973b-185">2. se não houver uma classe de tempo de execução válida, represente o objeto como uma instância de uma classe de tempo de execução sem nome que implementa exatamente a interface que a instância é conhecida a implementar, além das interfaces necessárias.</span><span class="sxs-lookup"><span data-stu-id="a973b-185">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="a973b-186">Um valor JavaScript é testado para determinar se ele é uma instância de uma classe de tempo de execução ou uma interface.</span><span class="sxs-lookup"><span data-stu-id="a973b-186">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="a973b-187">Se for, e o valor do Windows Runtime original implementar a interface, o objeto será passado para o tempo de execução do Windows.</span><span class="sxs-lookup"><span data-stu-id="a973b-187">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span> |  
| <span data-ttu-id="a973b-188">Classes de tempo de execução</span><span class="sxs-lookup"><span data-stu-id="a973b-188">Runtime classes</span></span> | `Object` | <span data-ttu-id="a973b-189">Os objetos no Windows Runtime podem ser instâncias de classes de tempo de execução que implementam uma ou mais interfaces.</span><span class="sxs-lookup"><span data-stu-id="a973b-189">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="a973b-190">Os objetos do Windows Runtime são representados em JavaScript como objetos.</span><span class="sxs-lookup"><span data-stu-id="a973b-190">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="a973b-191">A União de métodos, propriedades e eventos definidos em todas as interfaces implementadas da classe representam as propriedades nomeadas no protótipo do objeto JavaScript.</span><span class="sxs-lookup"><span data-stu-id="a973b-191">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="a973b-192">Os consumidores de JavaScript podem acessar qualquer membro da classe diretamente.</span><span class="sxs-lookup"><span data-stu-id="a973b-192">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="a973b-193">Os objetos do Windows Runtime têm um protótipo que contém todos os membros de todas as interfaces implementadas da classe de tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="a973b-193">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span> |  
  
## <span data-ttu-id="a973b-194">Consulte também</span><span class="sxs-lookup"><span data-stu-id="a973b-194">See Also</span></span>  

[<span data-ttu-id="a973b-195">Usar o tempo de execução do Windows em JavaScript</span><span class="sxs-lookup"><span data-stu-id="a973b-195">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- image links -->  

<!-- links -->  

[WindowsRuntimeJavascript]: /microsoft-edge/windows-runtime/using-the-windows-runtime-in-javascript "Usar o tempo de execução do Windows em JavaScript"  
