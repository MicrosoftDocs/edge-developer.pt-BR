---
description: Representação JavaScript de Tipos de Windows Runtime
title: Representação JavaScript de Tipos de Windows Runtime
ms.custom: ''
ms.date: 10/30/2020
ms.prod: microsoft-edge
ms.technology: windows-integration
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
ROBOTS: NOINDEX,NOFOLLOW
ms.openlocfilehash: 18a288f95e7211794e3ceb88d700026dae359b06
ms.sourcegitcommit: 6cf12643e9959873f8b5d785fd6158eeab74f424
ms.translationtype: MT
ms.contentlocale: pt-BR
ms.lasthandoff: 03/06/2021
ms.locfileid: "11399327"
---
# <a name="javascript-representation-of-windows-runtime-types"></a><span data-ttu-id="8d257-103">Representação em JavaScript dos tipos de Windows Runtime</span><span class="sxs-lookup"><span data-stu-id="8d257-103">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="8d257-104">A tabela a seguir descreve a maneira como o JavaScript representa os tipos do Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="8d257-104">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="8d257-105">Os recursos do Windows Runtime não estão disponíveis para aplicativos executados no Internet Explorer.</span><span class="sxs-lookup"><span data-stu-id="8d257-105">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <a name="windows-runtime-types-in-javascript"></a><span data-ttu-id="8d257-106">Tipos do Windows Runtime em JavaScript</span><span class="sxs-lookup"><span data-stu-id="8d257-106">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="8d257-107">Tipo do Windows Runtime</span><span class="sxs-lookup"><span data-stu-id="8d257-107">Windows Runtime type</span></span> | <span data-ttu-id="8d257-108">Representação javascript</span><span class="sxs-lookup"><span data-stu-id="8d257-108">JavaScript representation</span></span> | <span data-ttu-id="8d257-109">Descrição</span><span class="sxs-lookup"><span data-stu-id="8d257-109">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="8d257-110">Um Windows Runtime `Uint8` é um inteiro de 8 bits não assinado, representado como um número JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-110">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="8d257-111">Um valor JavaScript é convertido primeiro em um número JavaScript e, em seguida, `ToUint32` o processo é seguido.</span><span class="sxs-lookup"><span data-stu-id="8d257-111">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="8d257-112">Se o valor do número JavaScript estiver fora do intervalo de um Uint8, o resultado terá o modulo 2^8 aplicado.</span><span class="sxs-lookup"><span data-stu-id="8d257-112">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="8d257-113">Um Windows Runtime é um inteiro assinado de `Int32` 32 bits, representado como um número JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-113">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="8d257-114">Um valor JavaScript é convertido primeiro em um número JavaScript e, em seguida, `ToInt32` o processo é seguido.</span><span class="sxs-lookup"><span data-stu-id="8d257-114">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="8d257-115">Se o valor do número JavaScript estiver fora do intervalo de um , o resultado terá `Int32` o modulo 2^16 aplicado.</span><span class="sxs-lookup"><span data-stu-id="8d257-115">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="8d257-116">Um Windows Runtime é um inteiro assinado de 64 bits, representado como um número padrão se estiver dentro do `Int64` intervalo \[-2^53, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="8d257-116">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="8d257-117">Se ele estiver fora desse intervalo, ele será representado como um valor de número que tem um armazenamento de backing personalizado que mantém os 64 bits completos de dados inteiros.</span><span class="sxs-lookup"><span data-stu-id="8d257-117">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="8d257-118">Todas as operações matemáticas nesses valores de número int64 personalizados causam a conversão do valor em um número padrão no intervalo \[-2^53, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="8d257-118">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="8d257-119">Se o valor estiver fora desse intervalo, será gerado um erro de tipo.</span><span class="sxs-lookup"><span data-stu-id="8d257-119">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="8d257-120">As exceções a esse processo de conversão são as operações , , e, que comparam os argumentos com base nos 64 bits completos quando passados dois valores representados `==` `===` de `SameValue` `RelationalComparison` 64 bits.</span><span class="sxs-lookup"><span data-stu-id="8d257-120">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="8d257-121">Se um dos argumentos for um número JavaScript padrão, essas operações também converterão o argumento em um número JavaScript antes da comparação.</span><span class="sxs-lookup"><span data-stu-id="8d257-121">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="8d257-122">Um valor JavaScript é atribuído diretamente se for um valor Int64 em si; caso contrário, o resultado da aplicação `ToInteger` da conversão no valor é passado para o Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="8d257-122">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="8d257-123">Se o tipo de coerção falhar, uma exceção será retornada.</span><span class="sxs-lookup"><span data-stu-id="8d257-123">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="8d257-124">Um Windows Runtime é um inteiro de 64 bits não assinado, representado como um número padrão se ele estiver dentro do intervalo `Uint64` \[0, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="8d257-124">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="8d257-125">Se ele estiver fora desse intervalo, ele será representado como um número que tem um armazenamento de backing personalizado que mantém os 64 bits completos de dados inteiros não assinados.</span><span class="sxs-lookup"><span data-stu-id="8d257-125">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="8d257-126">Todas as operações matemáticas nesses valores Uint64 personalizados causam a conversão do valor em um número padrão no intervalo \[0, 2^53\].</span><span class="sxs-lookup"><span data-stu-id="8d257-126">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="8d257-127">Se o valor estiver fora desse intervalo, será gerado um erro de tipo.</span><span class="sxs-lookup"><span data-stu-id="8d257-127">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="8d257-128">As exceções a esse processo de conversão são as operações , , e, que comparam os argumentos com base nos 64 bits completos quando passaram dois valores de `==` `===` `SameValue` `RelationalComparison` 64 bits.</span><span class="sxs-lookup"><span data-stu-id="8d257-128">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="8d257-129">Se um dos argumentos for um número JavaScript padrão, essas operações também converterão o argumento em um número JavaScript antes da comparação.</span><span class="sxs-lookup"><span data-stu-id="8d257-129">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="8d257-130">Um valor JavaScript será atribuído diretamente se for um valor Uint64 em si; caso contrário, o resultado da aplicação da conversão no valor, seguido de tomar `ToInteger` o modulo 2^52, será passado para o Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="8d257-130">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="8d257-131">Se a conversão de tipo falhar, uma exceção será retornada.</span><span class="sxs-lookup"><span data-stu-id="8d257-131">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="8d257-132">Um Windows Runtime é um número de ponto flutuante de 32 bits, representado como um número JavaScript escolhendo o valor de precisão dupla mais próximo ao valor de `Single` precisão única.</span><span class="sxs-lookup"><span data-stu-id="8d257-132">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="8d257-133">Um valor JavaScript é convertido em um número JavaScript e, em seguida, o intervalo verificado para garantir que o valor possa ser representado em um número de ponto flutuante IEEE 754 de precisão única.</span><span class="sxs-lookup"><span data-stu-id="8d257-133">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="8d257-134">Se a verificação de conversão ou intervalo falhar, um erro de empacotamento será retornado.</span><span class="sxs-lookup"><span data-stu-id="8d257-134">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="8d257-135">Um Windows Runtime é um número de ponto flutuante de `Double` 64 bits.</span><span class="sxs-lookup"><span data-stu-id="8d257-135">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="8d257-136">é usado para converter um Windows Runtime `Double` em um número JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-136">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="8d257-137">Se a conversão falhar, um erro de empacotamento será retornado.</span><span class="sxs-lookup"><span data-stu-id="8d257-137">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="8d257-138">Um Tempo de Execução do Windows é um valor de 8 bits onde zero é e qualquer valor diferente de zero é , representado `Boolean` `false` como um `true` JavaScript `Boolean` .</span><span class="sxs-lookup"><span data-stu-id="8d257-138">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="8d257-139">Um valor JavaScript é convertido pela primeira vez em um JavaScript `Boolean` por `ToBoolean` .</span><span class="sxs-lookup"><span data-stu-id="8d257-139">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="8d257-140">Isso significa que uma função do Windows Runtime declarada como `void func(BOOL);` poderia ser escrita em JavaScript como `obj.func("test");` .</span><span class="sxs-lookup"><span data-stu-id="8d257-140">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="8d257-141">A função Tempo de Execução do Windows é chamada com o `BOOL` parâmetro passado como `TRUE` .</span><span class="sxs-lookup"><span data-stu-id="8d257-141">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="8d257-142">Se a conversão falhar, um erro de empacotamento será retornado.</span><span class="sxs-lookup"><span data-stu-id="8d257-142">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="8d257-143">Um Windows Runtime é um caractere Unicode de 16 bits, representado como uma cadeia de `Char16` caracteres JavaScript que contém um único caractere.</span><span class="sxs-lookup"><span data-stu-id="8d257-143">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="8d257-144">Um valor JavaScript é convertido em uma cadeia de caracteres JavaScript e, em seguida, verificado para garantir que a cadeia de caracteres seja `ToString` apenas um único caractere longo.</span><span class="sxs-lookup"><span data-stu-id="8d257-144">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="8d257-145">O caractere único é passado como o `Char16` valor.</span><span class="sxs-lookup"><span data-stu-id="8d257-145">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="8d257-146">Se a verificação de conversão ou comprimento falhar, um erro de empacotamento será retornado.</span><span class="sxs-lookup"><span data-stu-id="8d257-146">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="8d257-147">Um Windows Runtime `String` é uma sequência imutável de `Char16` objetos.</span><span class="sxs-lookup"><span data-stu-id="8d257-147">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="8d257-148">As cadeias de caracteres são representadas como objetos `String` JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-148">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="8d257-149">As cadeias de caracteres do Windows Runtime não têm valores diferentes e a cadeia de `null` caracteres vazia \(""\).</span><span class="sxs-lookup"><span data-stu-id="8d257-149">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="8d257-150">e valores de cadeia de caracteres vazios são convertidos para a cadeia de caracteres vazia JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-150">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="8d257-151">**OBSERVAÇÃO**: quando JavaScript é passado para um parâmetro do Windows Runtime que espera uma cadeia de caracteres, ele produz o valor de cadeia de caracteres "null", não ou a cadeia de `null` caracteres `null` vazia \(""\).</span><span class="sxs-lookup"><span data-stu-id="8d257-151">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="8d257-152">As cadeias de caracteres passadas para o Windows Runtime sempre devem ser instauidas para a cadeia de caracteres vazia.</span><span class="sxs-lookup"><span data-stu-id="8d257-152">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="8d257-153">Um Windows Runtime é um inteiro de 32 bits \(assinado ou não assinado\) que tem um conjunto `Enumeration` associado de constantes nomeadas.</span><span class="sxs-lookup"><span data-stu-id="8d257-153">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="8d257-154">Em JavaScript, uma enumeração é representada como um objeto que contém um campo somente leitura para cada valor nomeado.</span><span class="sxs-lookup"><span data-stu-id="8d257-154">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="8d257-155">Os valores de enumeração são convertidos em números JavaScript conforme definido anteriormente `Int32` para `UInt32` e .</span><span class="sxs-lookup"><span data-stu-id="8d257-155">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="8d257-156">Quando um valor JavaScript é convertido em uma enumeração do Windows Runtime, ele é convertido, truncado e marcado como descrito anteriormente.</span><span class="sxs-lookup"><span data-stu-id="8d257-156">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="8d257-157">No entanto, o valor resultante não é verificado em relação aos valores nomeados definidos especificados na enumeração.</span><span class="sxs-lookup"><span data-stu-id="8d257-157">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="8d257-158">Um Windows Runtime `Structure` é uma coleção heterogênea de campos de dados nomeados.</span><span class="sxs-lookup"><span data-stu-id="8d257-158">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="8d257-159">Em JavaScript, uma estrutura é representada como um objeto JavaScript que contém uma propriedade nomeada para cada campo na estrutura.</span><span class="sxs-lookup"><span data-stu-id="8d257-159">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="8d257-160">Se a conversão de qualquer valor de estrutura falhar, um erro de empacotamento será retornado.</span><span class="sxs-lookup"><span data-stu-id="8d257-160">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="8d257-161">Campos no objeto JavaScript que não têm equivalentes na estrutura do Windows Runtime são ignorados.</span><span class="sxs-lookup"><span data-stu-id="8d257-161">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="8d257-162">**OBSERVAÇÃO**: uma estrutura do Windows Runtime não pode ser instaurou em JavaScript com a `new` palavra-chave.</span><span class="sxs-lookup"><span data-stu-id="8d257-162">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="8d257-163">Um Windows Runtime `Array` é convertido em uma matriz JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-163">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="8d257-164">No entanto, as matrizes do Windows Runtime não são resizáveis, portanto, algumas operações de matriz JavaScript não são possíveis.</span><span class="sxs-lookup"><span data-stu-id="8d257-164">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="8d257-165">A propriedade interna de uma matriz convertida não está definida como "Matriz" porque isso não é permitido pela especificação `[[Class]]` ECMAScript 5.</span><span class="sxs-lookup"><span data-stu-id="8d257-165">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="8d257-166">Isso significa que `Array.isArray(v)` retorna `false` .</span><span class="sxs-lookup"><span data-stu-id="8d257-166">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="8d257-167">Um valor JavaScript é convertido em uma matriz do Windows Runtime da seguinte forma: se o valor for ou , ele `null` será convertido em um nativo `undefined` `null` .</span><span class="sxs-lookup"><span data-stu-id="8d257-167">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="8d257-168">Se sua propriedade interna for "Array", ela será copiada para uma matriz nativa e uma referência a `[[Class]]` essa matriz será passada.</span><span class="sxs-lookup"><span data-stu-id="8d257-168">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="8d257-169">Se for uma matriz convertida, conforme descrito anteriormente, a matriz nativa subjacente será passada.</span><span class="sxs-lookup"><span data-stu-id="8d257-169">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="8d257-170">**OBSERVAÇÃO**: passar um valor de matriz JavaScript para um método do Windows Runtime causa uma cópia completa da matriz.</span><span class="sxs-lookup"><span data-stu-id="8d257-170">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="8d257-171">Delegar \(retorno de chamada de função\)</span><span class="sxs-lookup"><span data-stu-id="8d257-171">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="8d257-172">Um representante do Windows Runtime é uma referência a um único método.</span><span class="sxs-lookup"><span data-stu-id="8d257-172">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="8d257-173">Um representante do Windows Runtime é representado em JavaScript como um JavaScript , que é garantido para `Function` ser chamado no thread apropriado.</span><span class="sxs-lookup"><span data-stu-id="8d257-173">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="8d257-174">Quando o `Function` é invocado, os argumentos são convertidos para os tipos de parâmetros equivalentes do Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="8d257-174">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="8d257-175">Se houver menos argumentos JavaScript do que `in` parâmetros de representante, a chamada delegada falhará.</span><span class="sxs-lookup"><span data-stu-id="8d257-175">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="8d257-176">Se houver mais argumentos JavaScript do que parâmetros no representante, os `in` argumentos JavaScript extras serão ignorados.</span><span class="sxs-lookup"><span data-stu-id="8d257-176">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="8d257-177">Depois que o representante é invocado, `out` os parâmetros são convertidos em tipos JavaScript e retornados.</span><span class="sxs-lookup"><span data-stu-id="8d257-177">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="8d257-178">Se um objeto de função JavaScript nativo for convertido em um representante do Windows Runtime, ele será envolvido em um representante do Windows Runtime do tipo correspondente.</span><span class="sxs-lookup"><span data-stu-id="8d257-178">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="8d257-179">Quando o representante é invocado, os `in` parâmetros são convertidos em tipos JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-179">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="8d257-180">Depois que o representante é invocado, o valor de retorno é convertido para `out` os parâmetros do representante.</span><span class="sxs-lookup"><span data-stu-id="8d257-180">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="8d257-181">Interface</span><span class="sxs-lookup"><span data-stu-id="8d257-181">Interface</span></span> | `Object` | <span data-ttu-id="8d257-182">Interfaces e grupos de interface não são representados diretamente em JavaScript como objetos.</span><span class="sxs-lookup"><span data-stu-id="8d257-182">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="8d257-183">No entanto, as interfaces são representadas como parâmetros e retornam tipos de métodos do Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="8d257-183">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="8d257-184">Uma instância de interface do Windows Runtime é convertida da seguinte forma:</span><span class="sxs-lookup"><span data-stu-id="8d257-184">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="8d257-185">1. Se houver uma classe de tempo de execução válida, represente o objeto como uma instância dessa classe.</span><span class="sxs-lookup"><span data-stu-id="8d257-185">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="8d257-186">2. Se não houver uma classe de tempo de execução válida, represente o objeto como uma instância de uma classe de tempo de execução sem nome que implementa exatamente a interface que a instância é conhecida por implementar, além de suas interfaces necessárias.</span><span class="sxs-lookup"><span data-stu-id="8d257-186">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="8d257-187">Um valor JavaScript é testado para determinar se é uma instância de uma classe de tempo de execução ou uma interface.</span><span class="sxs-lookup"><span data-stu-id="8d257-187">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="8d257-188">Se for, e o valor original do Windows Runtime implementar a interface, o objeto será passado para o Windows Runtime.</span><span class="sxs-lookup"><span data-stu-id="8d257-188">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="8d257-189">Classes de tempo de execução</span><span class="sxs-lookup"><span data-stu-id="8d257-189">Runtime classes</span></span> | `Object` | <span data-ttu-id="8d257-190">Os objetos no Windows Runtime podem ser instâncias de classes de tempo de execução que implementam uma ou mais interfaces.</span><span class="sxs-lookup"><span data-stu-id="8d257-190">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="8d257-191">Os objetos do Windows Runtime são representados em JavaScript como objetos.</span><span class="sxs-lookup"><span data-stu-id="8d257-191">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="8d257-192">A união de métodos, propriedades e eventos definidos em todas as interfaces implementadas da classe representa as propriedades nomeadas no protótipo do objeto JavaScript.</span><span class="sxs-lookup"><span data-stu-id="8d257-192">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="8d257-193">Os consumidores JavaScript podem acessar qualquer membro da classe diretamente.</span><span class="sxs-lookup"><span data-stu-id="8d257-193">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="8d257-194">Os objetos do Windows Runtime têm um protótipo que contém todos os membros de todas as interfaces implementadas da classe de tempo de execução.</span><span class="sxs-lookup"><span data-stu-id="8d257-194">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <a name="see-also"></a><span data-ttu-id="8d257-195">Veja também</span><span class="sxs-lookup"><span data-stu-id="8d257-195">See also</span></span>  

[<span data-ttu-id="8d257-196">Usando o Tempo de Execução do Windows no JavaScript</span><span class="sxs-lookup"><span data-stu-id="8d257-196">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "Usando o Tempo de Execução do Windows no JavaScript | Microsoft Docs"  
